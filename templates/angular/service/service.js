import * as API_CONSTANTS from '../../constants/api.js';

/**
 * Service - {%=o.exports%}
 *
 * {%=o.description%}
 *
 * {%=o.ticket%}
 */

export default class {%=o.exports%} {

  /**
   * constructor
   *
   * @param {function} $http - Angular HTTP
   * @param {function} $q - implementation of promises/deferred
   */
  constructor($http, $q) {
    this.$http = $http;
    this.$q = $q;
  }

  saveData() {
    
  }
}
